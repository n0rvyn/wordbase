---
import BaseLayout from '../../layouts/BaseLayout.astro';
import WritingIndexBody from '../../components/page-bodies/WritingIndexBody.astro';
import { loadWritingIndexData } from '../../lib/writing';
import { t } from '../../lib/i18n.js';

const data = await loadWritingIndexData('zh');
---

<BaseLayout title="写作" titleEn={t('title.writing').en} description="关于设计取舍、工程实践与独立开发。" hasEnTwin>
  <WritingIndexBody locale="zh" {...data} />
</BaseLayout>
