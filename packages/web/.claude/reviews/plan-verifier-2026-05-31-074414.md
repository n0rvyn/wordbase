## Plan Verification Summary
**Status:** complete
**Plan:** /Users/norvyn/Code/Projects/wordbase/docs/06-plans/2026-05-31-phase3-writing-article-plan.md
**Started:** 2026-05-31-074414

### Plan classification
- 功能开发 (new `/writing` page + helpers) → S1 + T1
- UI 开发 (new page + article rebuild + 2 component restyles) → S1 + U1
- 多步骤执行 (8 task sections, compile/runtime deps) → S2
- 有设计文档 (Writing.html + Article.html) → DF
- 有 crystal ([D-001]–[D-010]) → CF
- 架构变更: borderline — two pure-logic helper modules + a page rebuild, data flow mirrors existing `home.ts`/`index.astro`, no new Service/Agent/Tool, no new event entry, no data-flow topology change. **AR run as a light pass; no architecture issues found (see AR section).**
- 安全/资源敏感: NOT matched on Phase-3-introduced surface (build-time read-only render). `**Threat model:** not applicable` → S3 inactive. One advisory on the justification's accuracy (see S3 note).
- 有 Bug diagnosis: `not applicable` → BD skipped.

### Contract v2 structural checks
- `contract_version: 2` present. ✅ Plan-level `## Impact Map` present (6 rows: User/Data path, Shared surfaces, Existing consumers, Must remain unchanged, Regression checks).
- Every non-doc task carries `Expected outcome`, `Touched surface`, `Regression shield`, `Task Contract`, and `**Maps to Impact Map:**`. ✅
- Tasks touching UI/data/shared surfaces carry `Real path verify` (in Task Contract) or an explicit n/a justification (Task 1/2 pure logic → "verified in Task 3/4 build"). ✅
- `docs/00-AI-CONTEXT.md` absent (confirmed; plan's doc_drift yellow is accurate). Plan cites design refs + crystal directly per task. Legacy-acceptable for naming; no `## Language` glossary to cross-check. No blocking issue.
- No `CLAUDE.md`/`AGENTS.md` heading conflict in scope (project-level global rules only).
- Out-of-scope archive `dev-workflow/.out-of-scope/` does not exist → no rejected ideas to suppress.

---

### [S1] 具体候选错误验证

**[断言 1]** Task 5 step 2 写 `.share button{}` 选择器（"mirror Article.html `.share button`"），但 Twitter 控件是 `<a class="share-btn">`（ShareButtons.astro:14），WeChat/Copy 才是 `<button>`（:19/:25）。若按字面 `.share button` 选择器，Twitter 不会被圆形 hairline 样式命中，渲染为无样式链接，与 Expected outcome "three share controls render as 34px circular" 矛盾。
验证：读取 ShareButtons.astro:14/19/25 + Article.html:210 `.share button{width:34px;...}`。三个控件共享 `.share-btn` class（grep 确认全部 3 个都带 `class="share-btn"`）。
结果：❌ 断言成立 [C:90] → 计划需补充：Task 5 step 2 明确以 `.share-btn`（三控件共有的 class）为圆形样式键，而非 `button` 元素选择器；否则 Twitter `<a>` 漏样式。

**[断言 2]** Task 5 step 3 "keep the `<script>` handlers unchanged" + step 2 只替换 scoped `<style>`，但微信二维码 overlay 在 `<script>` 的 `innerHTML` 内联了 6 处死 token（ShareButtons.astro:106-109：`var(--color-paper-white)`/`var(--color-ink)`/`var(--color-paper-warm)`/`var(--color-rule)`/`var(--color-ink-faint)` + `var(--font-sans)`）。这些 token 在全项目零定义（`grep -c '--color-' src/` = 0），故 overlay 弹层会以浏览器默认值渲染（白底默认字体），与"restyle to hairline"矛盾，且属"做一半"。
验证：grep ShareButtons.astro 第 106-109 行内联 token；grep -c '--color-' src/ → 0（无任何定义、无 fallback）。
结果：❌ 断言成立 [C:92] → 计划需补充：Task 5 增加一步，把 `<script>` overlay innerHTML 的内联 `--color-*`/`--font-sans` 换成新 token（`--surface`/`--ink`/`--surface-2`/`--line`/`--ink-3`/`--font-ui`）。这是样式更新非逻辑改动，Non-goal 只保护"share targets / copy/QR logic"，允许且必须。

**[断言 3]** Task 6 step 2 "Replace Tailwind utility classes... add scoped `<style>`"，但 CommentSection 的评论列表由 `<script>` 的 `renderComment()` 用 `innerHTML` 注入（CommentSection.astro:64-75，带 `bg-white border rounded-lg p-4` 等 Tailwind class）。Astro scoped `<style>` 不作用于运行时 `innerHTML` 注入的 DOM（与 prose `set:html` 同理）——若仅给静态 markup 加 scoped style，动态注入的评论行仍无样式。
验证：CommentSection.astro:64-75 renderComment 返回 Tailwind 字符串；同 `:global` 原理（plan 自身 pre-flight risk line 32 + [D-009] 已认知 set:html 不被 scoped 样式触达）。
结果：✅ 断言不成立 [C:88] → Task 6 step 3 已明确 "restyle the `renderComment` HTML string (swap Tailwind classes for new-token inline styles / classes)"，即用内联 style 或 global class 处理动态注入行。计划已覆盖此陷阱。（提示：若 step 3 选 class 而非内联 style，class 必须放进 `<style is:global>`；建议在 step 3 注明，避免实现时落回 scoped。）

**[断言 4]** Task 4 step 7 把所有 `.prose *` 规则放进 `<style is:global>`。但 tokens.css:155 已有全局 `.prose{font-family:var(--font-read);font-size:var(--type-read);line-height:1.95;color:var(--ink)}` 与 :160 全局 `code{}`。Article 设计要求 prose `font-size:18px`（Article.html `.prose{font-size:18px}`），与 tokens.css 的 `var(--type-read)` 可能不同值——若 Article 页 global `.prose` 规则特异度/顺序不压过 tokens.css，字号会回落到全局值。
验证：tokens.css:155 全局 `.prose` 定义；Article.html prose `font-size:18px`。两条都是单 class 特异度；Astro `<style is:global>` 注入顺序晚于 tokens.css import → 后定义胜出，18px 生效。
结果：✅ 断言不成立 [C:82] → 同特异度下后加载者胜，页面 global `.prose` 覆盖 tokens.css。但存在脆弱性：依赖加载顺序。建议 Task 4 step 7 注明"页面 global prose 规则需确保覆盖 tokens.css:155 既有 `.prose`/`code` 全局规则（同特异度靠加载顺序，必要时提级）"，降低后续回归风险。advisory，不阻塞。

**[断言 5]** Task 3 step 1 构建 post→slugs map：循环 `getCategories()` → 每分类 `getPosts({category, limit:10000})`，累积每篇全部 slug。`index.astro:31-36` 的既有模式用 `if (!postCat.has(p.id))` 只取首个分类（单值）。若 Task 3 照抄此模式会丢失多分类（25/126 篇），导致 `data-cats` 只含一个 slug，筛选错误隐藏。
验证：index.astro:34 `if (!postCat.has(p.id)) postCat.set(...)` 确为 first-only 单值模式；plan Task 3 step 1 明确写 "accumulating ALL slugs per post id into Map<string,string[]> (NOT first-only — 25 posts are multi-category)"。
结果：✅ 断言不成立 [C:90] → 计划 step 1 显式纠正了 index.astro 的 first-only 模式，要求累积全部 slug 到数组。[D-002] 已覆盖。计划在此处正确避开了"照抄既有模式"的陷阱。

**[断言 6]** Task 5 Expected outcome 要求三控件 "render as 34px circular"，且 step 3 要 "keep `<script>` handlers unchanged"。但 copy 按钮内含 `<span id="copy-text">复制链接</span>`（ShareButtons.astro:29），脚本靠把该 span 的 textContent 换成 "已复制" 做复制反馈（:83-86）。34px 圆内塞不下 "复制链接"：保留 span → 非圆形（违反 Expected outcome）；CSS 隐藏 span → "已复制" 反馈不可见（坏反馈）；删 span → 违反 "script unchanged" 且 `getElementById('copy-text')` 查空。Step 3 只提到删 `分享` 标签（`share-label`:12），从未处理 `copy-text`:29 → 三约束互相矛盾。
验证：ShareButtons.astro:29 copy-text span；:83-86 textContent 反馈逻辑；:12/:41 的 `share-label` 与 `copy-text` 是不同元素（grep 确认）。Article.html:240 设计自身的复制反馈用 innerHTML 换 checkmark SVG（无文字）。
结果：❌ 断言成立 [C:90] → 计划需补充：Task 5 删除 `copy-text` span，复制反馈改用设计的 icon-swap（换 checkmark SVG，Article.html:240 同款）。clipboard.writeText 不变，故 Non-goal "do not change copy/QR logic"（保护剪贴板动作，非反馈 DOM）满足；计划须显式授权此呈现层改动，避免实现者卡在 Non-goal 死锁。

S1 汇总：生成 6 条，成立 3 条（断言 1、2、6，均 C≥80），不成立 3 条。无 C<80 被过滤项。

---

### [S2] 失败反向推理

**[编译失败推理 1]**
假设失败：Task 4 `getStaticPaths` 传 `props:{ post, orderedSlugs, slugTitle }`，但 `interface Props` 仍是旧的 `{ post: Post }`（[slug].astro:16-18 现状），`astro check` 报 prop 类型不匹配 / `orderedSlugs` 未声明。
计划覆盖：⚠️ 部分 — Task 4 step 1 描述传新 props，step 8 删旧 markup，但未显式提"更新 `interface Props`"。astro check 会捕获（编译期），属低风险。
→ 建议补充：Task 4 step 1 注明同步更新 `interface Props` 以含 `orderedSlugs: string[]` 与 `slugTitle: Record<string,string>`。advisory（astro check 必然拦截，执行时自然修复）。

**[编译失败推理 2]**
假设失败：Task 1-impl/2-impl 的 helper 从 `./home` import `decodeEntities`、从 `./api` import `type Post`，但 plan Task 1-impl step 2 写 "import only `type Post` and `decodeEntities` from `./home`/`./api`"——`Post` 在 api.ts 导出（api.ts:3），`decodeEntities` 在 home.ts 导出（home.ts:18），路径分属两文件。若实现时从同一文件 import 两者会报缺失。
计划覆盖：✅ step 2 写法 "`./home`/`./api`" 暗示分别 import；两符号导出位置已 grep 确认（home.ts:18 / api.ts:3）。低风险。

**[运行时 Regression 推理 1]**
假设 regression：Task 3 step 6 chip 筛选脚本对 `.item.post[data-cats]` 操作，但 featured 卡（step 4）无 `data-cat`/`data-cats`。若脚本选择器误用 `.item.post`（featured 也可能带 `post` class）会把 featured 也隐藏。
操作路径：用户点 chip → 脚本 querySelectorAll → 若命中 featured → featured 消失。
计划覆盖：✅ step 4 明确 featured "No `data-cat`"，step 6 筛选 "for each `.item.post[data-cats]`"（带属性选择器，featured 无 data-cats 不被选中）。且 featured 用 `.post-feat` class 而非 `.item.post`。已覆盖。

**[运行时 Regression 推理 2]**
假设 regression：Task 4 进度条脚本 `bar.style.width = scrollTop/scrollHeight`。文章页同时被 BaseLayout 的 nav `.scrolled` 脚本监听 scroll（BaseLayout:135）。两个 scroll listener 共存。若 Task 4 脚本重复绑定 nav.scrolled 逻辑会双触发。
计划覆盖：✅ Task 4 step 6 明确 "nav `.scrolled` is already handled by BaseLayout — do not duplicate"。已覆盖。

**[运行时 Regression 推理 3]**
假设 regression：Home `全部文章` 链接改 `/` → `/writing`（Task 3 step 8）。若 `/writing` build 失败或未产出，Home 出现死链。
操作路径：用户在 Home 点"全部文章" → /writing 404。
计划覆盖：✅ Task 3 同一 task 同时创建 `/writing/index.astro` 与改链接；Regression checks 含 "emits ... + `/writing`"。原子性由 Task 3 边界保证。已覆盖。

S2 汇总：编译失败 2 条（1 条 advisory：interface Props 未显式更新；1 条已覆盖），运行时 regression 3 条全部已覆盖。无 must-revise。

---

### [T1] 测试覆盖验证

| 代码类型 | 有逻辑的 Task | 有测试覆盖 | 测试类型匹配 |
|---------|--------------|-----------|-------------|
| 业务逻辑 (category select / year group / density) | Task 1-impl | Task 1-tests | ✅ UT |
| 业务逻辑 (heading-ID inject / TOC / prev-next) | Task 2-impl | Task 2-tests | ✅ UT |
| UI 组装 (writing page) | Task 3 | astro check + dist grep (Task Contract) | ⚠️ 无 UT，page-level 用 build+grep |
| UI 组装 (article page) | Task 4 | astro check + dist grep | ⚠️ 无 UT，page-level 用 build+grep |
| 组件样式 (ShareButtons) | Task 5 | ⚠️ No test 标注 | ✅ 理由成立 (style-only, 逻辑不变) |
| 组件样式+文案 (CommentSection) | Task 6 | ⚠️ No test 标注 | ✅ 理由成立 (style+copy, fetch/post 逻辑不变) |

覆盖率：核心可测逻辑 (Task 1/2 helpers) 100% UT 覆盖，且 test-first（先写 red 测试）。
类型匹配：2/2 业务逻辑 task → Unit Test ✅。
- Task 3/4 是 UI 组装层，可测逻辑已全部抽到 writing.ts/article.ts（Task 3 的 `<!-- no-split -->` 注释明确说明）；page 层用 astro check + dist grep 验证，符合 "用户旅程/UI 用 build 验证" 原则。无 E2E 框架（plan 未引入 Playwright），交互手感留累积浏览器评审（已在每个 task 的 Manual/device verify 标注 `⚠️ 需浏览器验证`）。
- Task 5/6 标注 `⚠️ No test: {reason}` — 纯样式/纯文案改动，share/comment 逻辑与 element ID/name/data-* 全部保留（Regression shield grep 守护），理由成立。

T1 汇总：逻辑 task 2，有测试 2，类型匹配 2。无 T1 must-revise。Task 5/6 的 No-test 标注理由审核通过。

---

### [U1] Design Token 一致性验证

设计系统文件：`src/styles/tokens.css`（CSS 变量，非 DesignTokens.swift）。新 token 全部已定义：`--accent`(#3457B6) / `--on-accent` / `--line` / `--line-2` / `--ink`~`--ink-4` / `--surface`~`--surface-3` / `--font-mono`/`--font-display`/`--font-read`/`--font-ui` / `--gut` / `--maxw`。全部 grep 确认存在。

| Task | UI 值 | Token | 状态 |
|------|-------|-------|------|
| Task 3 chips | `.chip-f` border/radius/accent-sel | `--line-2`/`--accent`/`--on-accent` | ✅ 设计值与 tokens 一致 |
| Task 3 featured | `.post-feat`/`.tag`/`.read-link` | `--accent`/`--ink-2`/`--ink-3`/`--line` | ✅ |
| Task 4 progress | `.progress` 2px `var(--accent)` | `--accent` | ✅ |
| Task 4 toc active | `.toc a.active` 左 border `--accent` | `--accent`/`--line` | ✅ |
| Task 4 prose | `font-size:18px;line-height:1.95` | 18px 字面值 (设计明确) | ✅ Article.html 一致 |
| Task 5 share | 34px circle, `--line-2` hover `--accent` | `--line-2`/`--ink-3`/`--accent`/`--surface` | ✅ |
| Task 6 comments | hairline `1px solid var(--line)` | `--line` | ✅ |

⛔ 旧 token 残留风险（详见 S1 断言 1/2）：
- ShareButtons.astro 现用 `var(--color-*)`/`var(--font-sans)` 12 处，**零定义无 fallback**。Task 5 step 2 替换 scoped `<style>` 解决静态部分，但 step 3 保留的 `<script>` overlay innerHTML（:106-109）残留 5 个 `--color-*` + 1 个 `--font-sans` → 必须在 Task 5 增补一步清理。**must-revise（见 S1 断言 2）**。
- 计划 pre-flight risk line 36 与 Task 5 step 4 声称 `--color-*` "alias-resolve" 是**事实错误**（grep -c '--color-' src/ = 0，无 alias、无定义）。该误判正是 step 3 放过 script token 的原因。需作为计划准确性更正一并修订。

一致性补充检查（mirror 既有同类组件）：
- Task 3 复用 Home 的 `.post`/`.pdate`/`.viewall`/`.item-title`/`.item-sub` 模式（index.astro:354-406），新增 `.chip-f`/`.post-feat`/`.year-head`/`.post.compact` 均 mirror Writing.html 值。✅ 模式一致。

U1 汇总：检查 7 项设计值，全部有 token 支撑且与设计一致；缺失 0。旧 token 残留 2 处（overlay script 内联死 token → MR2；selector 键漏 `.share-btn` 致 Twitter `<a>` 漏样式 → MR1），外加 copy-text span 与 34px 圆形互斥 → MR4。建议给 Task 5 automated verify 增负向 grep `var(--color-`/`var(--font-sans)` = 0（精确模式）。

---

### [DF] 设计忠实度验证

设计文档：Writing.html + Article.html（HTML 原型，无 `## UX Assertions` 表 → DF 步骤 7 跳过，已被 plan 各 task 的 `⚠️ No UX ref` 正确标注）。

**步骤 1 双向映射：**
正向（设计要求 → task）：
- Writing `.topics`/`.chip-f` 筛选 → Task 3 ✅
- Writing `.post-feat` featured 卡 → Task 3 ✅
- Writing `.list`/`.item.post` 行 → Task 3 ✅
- Writing `.viewall` 全部分类链接 → Task 3 ✅
- Article `.art-grid`/`.rail`/`.toc`/`.share` → Task 4+5 ✅
- Article `.progress` 进度条 → Task 4 ✅
- Article `.reading`/`.dek`/`.byline`/`.prose`/`.endmark`/`.author-card`/`.pn-nav` → Task 4 ✅
- Article prose 子元素样式 (h2/h3/blockquote/code/pre/ul/li) → Task 4 step 7 (`:global`) ✅

反向（task → 设计）：所有 task 均映射到设计要求。Task 1/2 helper 是设计要求的支撑逻辑（heading-ID/TOC/年份分组），无杜撰 task。年份分组 + 密度递减是 [D-003] crystal 决策（设计稿无年份轴）→ 见 DF 步骤 5。

**步骤 2 设计锚点：** Task 3/4 均带 `Design ref:`/`Expected values:`/`Replaces:`/`Data flow:`/`Quality markers:`/`UX ref:`。Task 4 `Replaces:` 明确 "existing serif article layout (back-link/article-header/.prose serif)"。锚点完整。✅

**步骤 3 Gap 扫描：**
- Gap A（值错误）：抽验设计值——`.chip-f.sel`=accent填充、`.toc a.active`=左border accent、`.prose font-size:18px`、`.endmark ◆`=accent、`scroll-margin-top:96px`、share `34px/border-radius:50%`——全部出现在 plan Expected values。✅
- Gap B（未接入）：新组件 writing.ts/article.ts 都有 Data flow 路径（getPosts→helper→DOM / marked→injectHeadingIds→DOM）。✅
- Gap C（旧代码）：Task 4 `Replaces:` 列出旧 serif 版式；step 8 显式 "Remove the old serif markup/styles entirely"。✅
- Gap D（未建设）：正向映射无未覆盖设计要求。✅
- Gap E（退化）：进度条实现方式偏离设计——设计是 nav 内 `position:absolute;bottom:-1px`（Article.html:213），plan Task 4 step 3 改 `position:fixed;top:0` 页面自有元素。**此偏离由 [D-007] 显式授权**（进度条由文章页渲染，不塞进 BaseLayout nav）→ acknowledged，非 silent degradation。无 must-revise。无 "simplified/heuristic/placeholder/stub" 未标注关键词。

**步骤 4 隐含上下文：**
- 设计稿 5 主题（设计/工程/随笔/复盘）真实数据不存在 → plan [D-001] 改用真实高频分类。✅ 已处理。
- 设计 `data-cat` 单值 → 真实 25/126 多分类 → plan `data-cats` 复数（[D-002]）。✅ 已处理（隐含上下文：设计原型单分类假设不成立）。
- 设计 prose 用 `<style>`（HTML 原型内联生效）→ Astro `set:html` 不被 scoped 触达 → plan `:global`（[D-009]）。✅
- marked@12 标题无 id（REPL 验证）→ plan 构建期注入索引 id（[D-008]）。✅

**步骤 5 粒度审计：**
- 设计 Article 是单页一体；plan 拆为 helper(Task 2) + page(Task 4) + 2 组件(Task 5/6)。拆分未引入设计外中间状态（helper 是纯函数，组件是既有组件重样式）。✅
- 年份分组（设计无 → [D-003] 新增）：plan Task 3 step 5/6 引入 `.year-head` + 空年份隐藏逻辑，设计原型筛选脚本无此逻辑（grep 确认）。这是 crystal 授权的粒度增加，非设计丢失。✅

**步骤 6 边界场景：**
- 空 TOC（无 h2/h3 文章）→ Task 2-tests 明确 "Empty/no-heading input → {html:<same>, toc:[]}"。✅
- null publishedAt → Task 1-tests groupByYear "posts with null publishedAt go to trailing group year:0"。✅
- prev/next 端点 → selectAdjacent 首/尾返回 null，Task 4 step 4 "omit a card if null"。✅
- 多分类筛选匹配 → data-cats 列表匹配（[D-002]）。✅
- 边界覆盖充分。

DF 汇总：设计要求映射 8/8，缺失锚点 0，隐含上下文 4 项全覆盖，粒度问题 0（2 处 crystal 授权增加），边界场景 4 个全覆盖，UX 断言 n/a（无 UX Assertions 表，已正确标注 No UX ref）。Gap E 进度条偏离 = [D-007] acknowledged。**无 DF must-revise。**

---

### [CF] 决策忠实度验证

**步骤 1 决策覆盖：**
- [D-001] 高频分类 chip + 全部分类→/categories → Task 3 (step 3) ✅
- [D-002] 行 data-cats 空格分隔全部 slug → Task 3 (step 1/5) ✅
- [D-003] 年份分组 + 密度递减(2025/2024 full, 更早 compact) + 空年份隐藏 → Task 1 + Task 3 (step 5/6) ✅
- [D-004] featured = 最新单篇，在筛选外固定 → Task 3 (step 2/4) ✅
- [D-005] 分享保留三按钮，圆形 hairline 入 rail → Task 5 ✅
- [D-006] 评论保留，去 Tailwind + 新 token hairline + 中文文案 → Task 6 ✅
- [D-007] TOC/高亮/进度条 = 原生脚本，进度条由文章页渲染不入 BaseLayout nav → Task 4 (step 3/6) ✅
- [D-008] 标题 id 构建期注入，索引式 id → Task 2 ✅
- [D-009] prose 用 :global → Task 4 (step 7) ✅
- [D-010] 旧 serif 版式完全替换，无 Cormorant/朱红 → Task 4 (step 8) + 验证 grep `cormorant|#c23a22`=0 ✅

覆盖：10/10。

**步骤 2 否决方案反向检查：**
- "按距今 6mo/1yr 分层" 否决（会塌缩成全标题）→ plan 用年份轴([D-003])，无 task 实现距今分层。✅ No conflict
- "GitHub 式 slug id" 否决（中文空/冲突）→ plan 用索引 id([D-008])。✅ No conflict
- "/writing 分页" 否决（与客户端筛选互斥）→ plan Non-goals 明确 "No pagination (Phase 7)"。✅ No conflict
- "分享精简成 2 按钮" 否决（scope-guard）→ plan Task 5 Non-goals "Do not remove any of the three buttons"。✅ No conflict

**步骤 3 Scope 边界：**
plan header 无 `**Scope conflicts:**` 段落。
- OUT "/categories 重做" → plan 链接到现有 /categories，未改其样式（Task 3 Non-goals "No /categories restyle"）。✅ 无越界
- OUT "/archives /tags /page/[page]" → plan Must-remain-unchanged 明确 "keep old layout"。✅ 无越界
- 删除检查：Task 4 step 8 删旧 serif markup/styles — 被 [D-010] + IN "新 Article 模板" 授权。✅
  - LSP/grep 残留 caller 检查：旧 `.prose`(serif)/`.back-link`/`.article-header` 是 [slug].astro 页内私有 scoped class，无外部 import；ShareButtons/CommentSection 是被 import 的组件（grep 确认仅 [slug].astro 消费），未删除只重样式。无残留 caller。✅
- ShareButtons/CommentSection 行为保留（element ID/name/data-* 由 Regression shield grep 守护），仅样式变更——符合 [D-005]/[D-006]"只重样式不删功能"。✅

CF 汇总：决策覆盖 10/10，否决方案冲突 0，scope 越界 0，未授权删除 0。**无 CF must-revise。**

---

### [AR] 架构审查（light pass）

Phase 3 不引入新 Service/Agent/Tool/事件入口，不改数据流拓扑。新增两个纯函数 helper 模块（writing.ts/article.ts），数据流 mirror 既有 home.ts/index.astro 模式（build-time getPosts/getCategories → helper → astro DOM）。
- 入口冲突：无新入口。`/writing` 是新路由，与既有 `/`、`/posts/[slug]`、`/categories` 无冲突（Home "全部文章" 链接由 `/` 改指 `/writing`，原子改动）。✅
- 替代清单：Task 4 全量替换 [slug].astro serif 版式，Replaces 锚点 + step 8 删除完整。✅
- 组件复用：ShareButtons/CommentSection 沿用既有组件（仅重样式），未在调用方新建重叠实现（符合"改共享组件不脱离"原则）。✅

AR 汇总：架构 issue 0。

---

### [S3] 运行时语义与资源安全验证

`**Threat model:** not applicable` → S3 不激活（无安全/资源敏感分类命中 Phase-3 新增面；内容构建期只读渲染）。

**advisory（不阻塞，不计 must-revise）**：plan header 的 threat-model 理由 "content is build-time read-only from a trusted internal API" 对评论不准确——CommentSection 在**运行时** `loadComments()` fetch 用户生成内容并经 `innerHTML` 注入 `c.content`（CommentSection.astro:72，未转义）。但 Task 6 显式保留 render 逻辑不变 + 内容经审核 gate，Phase 3 未引入新攻击面，**不构成新 gap、不阻塞**。仅记录 header 理由的事实瑕疵，供后续 Phase 关注 XSS 面（既有 pre-Phase-3 行为）。

---

## Plan Verification Summary (rollup)

### 策略执行
- S1 具体候选错误：生成 6 条，成立 3 条（C≥80）；过滤 0（无 C<80）
- S2 失败反向推理：编译失败 2（1 advisory），运行时 regression 3（全覆盖）
- T1 测试覆盖：逻辑 task 2，有测试 2，类型匹配 2
- U1 Token 一致性：检查 7 项，缺失 0；旧 token 残留 1 处（overlay script → must-revise via S1-2）
- DF 设计忠实度：映射 8/8，缺失锚点 0，隐含上下文 4/4，粒度问题 0，边界 4/4，UX 断言 n/a，Gap E 进度条偏离 = [D-007] acknowledged
- CF 决策忠实度：覆盖 10/10，否决冲突 0，scope 越界 0，未授权删除 0
- BD：skipped（not applicable）
- AR 架构审查：issue 0
- S3 运行时语义：skipped（threat model n/a）；1 advisory（header 理由对评论 XSS 面表述不准，不阻塞）

### 必须修订（验证发现的确实问题）
1. [Task 5 step 2] 圆形 hairline 样式必须以 `.share-btn`（三控件共有 class）为选择器键，而非字面 `.share button` 元素选择器——否则 Twitter `<a class="share-btn">`（ShareButtons.astro:14，非 button）漏样式，违反 Expected outcome "three share controls render as 34px circular"。
   依据：S1 断言 1 [C:90]
2. [Task 5] 增补一步：清理 `<script>` 内微信 overlay innerHTML 的内联死 token（ShareButtons.astro:106-109 的 5×`--color-*` + 1×`--font-sans`，全项目零定义无 fallback），换成新 token。无定义无 fallback 的 CSS var 解析为 *invalid* → QR 卡片 background 变**透明**（透出背后的暗色 scrim），非"白底" = restyle 做一半。属样式更新，Non-goal 仅保护 share/copy/QR 逻辑，允许且必须。同时给 Task 5 的 automated verify 增补负向 grep（对齐 Task 6 的做法）：`grep -c 'var(--color-' src/components/ShareButtons.astro` = 0 且 `grep -c 'var(--font-sans)' src/components/ShareButtons.astro` = 0（用精确 `var(--color-` 模式，勿用裸 `color-`，否则误命中 `border-color`/`background-color`）——否则 MR2/MR4 可"计划修了执行漏了"无人拦截。
   依据：S1 断言 2 [C:92] + U1
3. [plan pre-flight risk line 36 + Task 5 step 4] 更正事实错误："`--color-*` tokens alias-resolve" 不成立——`grep -c '--color-' src/` = 0，无定义无 alias。此误判是 step 3 放过 script token 的根因，需与修订 2 一并更正。
   依据：U1（grep 证据）
4. [Task 5 step 3] 删除 copy 按钮的 `<span id="copy-text">复制链接</span>`（ShareButtons.astro:29），复制反馈改用设计的 icon-swap（换 checkmark SVG，Article.html:240 同款）；34px 圆形容不下文字标签，且当前脚本靠 textContent 换字做反馈（:83-86）与圆形按钮互斥。step 3 只处理了 `share-label`(:12)，漏了 `copy-text`(:29)，三约束（34px circular / script unchanged / 仅删 share-label）互相矛盾。clipboard.writeText 不变，Non-goal 满足；计划须显式授权此呈现层改动以解死锁。
   依据：S1 断言 6 [C:90] + U1

### 建议补充（验证过程中暴露的风险，advisory，不阻塞）
1. [Task 4 step 1] 注明同步更新 `interface Props` 含 `orderedSlugs: string[]` + `slugTitle: Record<string,string>`（astro check 必拦，执行时自然暴露）。依据：S2 编译推理 1
2. [Task 4 step 7] 注明页面 global `.prose`/`code` 规则需覆盖 tokens.css:155/:160 既有全局规则（同特异度靠加载顺序，必要时提级），降低字号回落风险。依据：S1 断言 4 [C:82]
3. [Task 6 step 3] 注明 renderComment 动态注入行若用 class（非内联 style）须放进 `<style is:global>`（scoped 不触达 innerHTML）。依据：S1 断言 3
4. [plan header threat model] 理由对评论运行时 XSS 面表述不准（CommentSection 运行时 innerHTML 注入未转义 c.content），Phase 3 不引入新面、不阻塞，记录供后续 Phase。依据：S3

### 无需修订（验证通过的关键项）
- 多分类 data-cats（[D-002]）：plan 显式纠正 index.astro 的 first-only 模式，累积全部 slug（S1 断言 5 ✅）
- :global prose（[D-009]）+ CommentSection 动态行重样式（S1 断言 3 ✅）
- 进度条 fixed/top（[D-007] 授权偏离设计的 nav 内 absolute，非降级）
- 旧 serif 版式完全替换（[D-010]，Replaces + 删除完整，无残留 caller）
- helper 测试 test-first + 100% 覆盖可测逻辑（T1 ✅）
- 全部 10 条 crystal 决策覆盖、4 条否决方案无冲突、scope 无越界（CF ✅）

### 低置信度发现 (C < 80)
- 无。

## Decisions
None.
（修订 1-3 是明确的实现缺陷修正，plan/design/crystal/code 共同确定了正确做法——补 selector 键、补 token 清理步、更正事实陈述——无 2+ 方案分歧，按修订指令处理，不生成 DP。）
