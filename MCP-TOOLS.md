# WordBase MCP — Tool Catalog

> Auto-generated by `pnpm mcp:catalog` — do not edit by hand.
> Source: `packages/api/src/mcp/tools.ts` · MCP server `wordbase-blog` v1.2.0 · 2026-06-20

**60 tools** across 8 groups.

## Index

- **Blog** (14): `blog_list_media` · `blog_upload_media` · `blog_delete_media` · `blog_list_comments` · `blog_moderate_comment` · `blog_reply_comment` · `blog_delete_comment` · `blog_analytics_overview` · `blog_analytics_top_posts` · `blog_analytics_trends` · `blog_content_stats` · `blog_trigger_build` · `blog_build_status` · `blog_manage_redirects`
- **Podcast** (13): `podcast_list_shows` · `podcast_create_show` · `podcast_publish_show` · `podcast_list_episodes` · `podcast_analytics` · `podcast_get_feedback` · `podcast_create_episode` · `podcast_upload_audio` · `podcast_publish_episode` · `podcast_update_show` · `podcast_update_episode` · `podcast_import_feed` · `podcast_upload_audio_from_url`
- **Apps** (7): `app_list` · `app_create` · `app_publish` · `app_update` · `app_discover` · `app_sync` · `app_sync_all`
- **Pages** (6): `page_list` · `page_get` · `page_create` · `page_update` · `page_delete` · `page_publish`
- **category** (4): `category_list` · `category_create` · `category_update` · `category_delete`
- **i18n** (3): `i18n_render` · `i18n_pending` · `i18n_put_cache`
- **post** (9): `post_list` · `post_get` · `post_search` · `post_create` · `post_update` · `post_publish` · `post_archive` · `post_delete` · `post_update_meta`
- **tag** (4): `tag_list` · `tag_create` · `tag_update` · `tag_delete`

## Blog (14)

### `blog_list_media`

List media library items

| Parameter | Type | Description |
| --- | --- | --- |
| `page` |  | Page number (default: 1) |
| `limit` |  | Items per page (default: 20) |

### `blog_upload_media`

Upload a file to the media library (base64 encoded)

| Parameter | Type | Description |
| --- | --- | --- |
| `filename` |  | Original filename |
| `content` |  | Base64 encoded file content |
| `mimeType` |  | MIME type (e.g., image/png, image/jpeg) |
| `altText` |  | Alt text for the image (optional) |

### `blog_delete_media`

Delete a media item

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Media ID to delete |

### `blog_list_comments`

List comments for a blog post

| Parameter | Type | Description |
| --- | --- | --- |
| `postId` |  | Post ID to list comments for |
| `status` |  | Filter by status: approved, pending, spam, trash (default: approved) |
| `page` |  | Page number (default: 1) |
| `limit` |  | Items per page (default: 20) |

### `blog_moderate_comment`

Moderate a comment (approve, spam, or trash)

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Comment ID to moderate |
| `action` |  | Action: approve, spam, or trash |

### `blog_reply_comment`

Reply to an existing comment

| Parameter | Type | Description |
| --- | --- | --- |
| `postId` |  | Post ID the comment belongs to |
| `parentId` |  | Parent comment ID to reply to |
| `authorName` |  | Author name for the reply |
| `content` |  | Reply content |

### `blog_delete_comment`

Delete a comment

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Comment ID to delete |

### `blog_analytics_overview`

Get blog analytics overview (total PV, today PV, active posts)

_No parameters._

### `blog_analytics_top_posts`

Get top posts by page views

| Parameter | Type | Description |
| --- | --- | --- |
| `limit` |  | Number of top posts to return (default: 10) |

### `blog_analytics_trends`

Get traffic trends over time

| Parameter | Type | Description |
| --- | --- | --- |
| `period` |  | Period: daily, weekly, or monthly (default: daily) |

### `blog_content_stats`

Get content statistics: publish frequency and tag distribution

_No parameters._

### `blog_trigger_build`

Trigger an Astro site rebuild

_No parameters._

### `blog_build_status`

Check current build status

_No parameters._

### `blog_manage_redirects`

List, create, or delete URL redirects

| Parameter | Type | Description |
| --- | --- | --- |
| `action` |  | Action: list, create, or delete |
| `fromPath` |  | Source path (for create) |
| `toPath` |  | Target path (for create) |
| `id` |  | Redirect ID (for delete) |

## Podcast (13)

### `podcast_list_shows`

List all podcast shows, optionally filtered by status

| Parameter | Type | Description |
| --- | --- | --- |
| `status` |  | Filter by status: draft or published |
| `page` |  | Page number (default: 1) |
| `limit` |  | Items per page (default: 20) |

### `podcast_create_show`

Create a new podcast show with title, description, and owner info

| Parameter | Type | Description |
| --- | --- | --- |
| `title` |  | Show title |
| `description` |  | Show description |
| `ownerEmail` |  | Owner email for iTunes feed |
| `ownerName` |  | Owner name for iTunes feed |
| `author` |  | Author name |
| `language` |  | Language code (default: zh-CN) |
| `category` |  | iTunes category |
| `explicit` |  | 1 if explicit content, 0 otherwise |
| `coverImage` |  | Cover image URL |
| `appleUrl` |  | Apple Podcasts show URL |
| `spotifyUrl` |  | Spotify show URL |
| `slug` |  | URL slug (auto-generated if not provided) |

### `podcast_publish_show`

Publish a podcast show so it appears on the public site

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Podcast show ID |

### `podcast_list_episodes`

List episodes for a podcast show, optionally filtered by status

| Parameter | Type | Description |
| --- | --- | --- |
| `podcastId` |  | Podcast show ID |
| `status` |  | Filter by status: draft or published |
| `page` |  | Page number (default: 1) |
| `limit` |  | Items per page (default: 20) |

### `podcast_analytics`

Podcast consumption analytics: deduped downloads (total + windowed), active-subscriber estimate, top episodes, and feed-poll client distribution

| Parameter | Type | Description |
| --- | --- | --- |
| `days` |  | Window in days for windowed downloads (default: 30) |
| `limit` |  | Max top episodes / clients to return (default: 10) |

### `podcast_get_feedback`

Get recent listener feedback (reaction + category + note) across episodes, for podcast topic-reward and viewpoint-falsification signals. Read-only.

| Parameter | Type | Description |
| --- | --- | --- |
| `days` |  | Look back this many days (default: 7). Ignored if `since` is given. |
| `since` |  | Unix epoch seconds; return feedback created at or after this time. |
| `episodeId` |  | Filter to a single episode id. |
| `limit` |  | Max rows (default: 200, max: 1000). |

### `podcast_create_episode`

Create or update a podcast episode (idempotent when an external id is given — safe for automated re-delivery)

| Parameter | Type | Description |
| --- | --- | --- |
| `podcastId` |  | Podcast show ID |
| `title` |  | Episode title |
| `audioUrl` |  | URL of the audio file |
| `audioSize` |  | Audio file size in bytes |
| `duration` |  | Duration in seconds |
| `summary` |  | Short summary |
| `showNotes` |  | Full show notes (Markdown) |
| `episodeNumber` |  | Episode number |
| `seasonNumber` |  | Season number |
| `coverImage` |  | Episode cover image URL |
| `transcript` |  | Transcript text |
| `episodeType` |  | Episode type: full, trailer, or bonus |
| `explicit` |  | 1 if explicit, 0 otherwise |
| `publishedAt` |  | Publish date as a unix epoch (seconds) |
| `audioType` |  | Audio MIME type, e.g. audio/mpeg, audio/wav, audio/mp4 (defaults to audio/mpeg) |
| `externalSource` |  | External source identifier (e.g. "adam") |
| `externalId` |  | External episode ID for idempotent upsert |
| `slug` |  | URL slug (auto-generated if not provided) |

### `podcast_upload_audio`

Upload an audio file for a podcast episode (base64 encoded)

| Parameter | Type | Description |
| --- | --- | --- |
| `filename` |  | Original filename (e.g. episode-1.mp3) |
| `base64` |  | Base64 encoded audio file content |
| `mimeType` |  | MIME type (e.g. audio/mpeg) |

### `podcast_publish_episode`

Publish a podcast episode so it appears in the RSS feed and on the site

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Episode ID |

### `podcast_update_show`

Update a podcast show's metadata (title/description/slug/author/owner/category/cover/etc.). Triggers a site rebuild.

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Podcast show ID |
| `title` |  | Show title |
| `description` |  | Show description |
| `slug` |  | URL slug |
| `author` |  | Author name |
| `ownerName` |  | Owner name for iTunes feed |
| `ownerEmail` |  | Owner email for iTunes feed |
| `category` |  | iTunes category |
| `coverImage` |  | Cover image URL |
| `language` |  | Language code (e.g. zh-CN) |
| `copyright` |  | Copyright line |
| `link` |  | Show website link |
| `appleUrl` |  | Apple Podcasts show URL |
| `spotifyUrl` |  | Spotify show URL |
| `explicit` |  | 1 if explicit content, 0 otherwise |
| `status` |  | Status: draft or published |

### `podcast_update_episode`

Update a podcast episode (title/summary/show notes/transcript/episode number/type/publishedAt/etc.). Rebuilds the site when the episode is published.

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Episode ID |
| `title` |  | Episode title |
| `summary` |  | Short summary |
| `showNotes` |  | Full show notes (HTML or Markdown) |
| `transcript` |  | Transcript text |
| `episodeNumber` |  | Episode number |
| `seasonNumber` |  | Season number |
| `episodeType` |  | Episode type: full, trailer, or bonus |
| `explicit` |  | 1 if explicit, 0 otherwise |
| `duration` |  | Duration in seconds |
| `coverImage` |  | Episode cover image URL |
| `publishedAt` |  | Publish date as a unix epoch (seconds) |
| `status` |  | Status: draft or published |

### `podcast_import_feed`

Import an external podcast RSS feed (e.g. Anchor/Spotify): upserts every episode (idempotent by guid), keeping the original audio URLs and publish dates. Episodes import as draft unless status=published. When importing into an EXISTING show, the show's own metadata (title/cover/owner/etc.) is left untouched unless syncShow=1.

| Parameter | Type | Description |
| --- | --- | --- |
| `feedUrl` |  | URL of the external RSS feed |
| `podcastId` |  | Target show ID to import into; if omitted, a new show is created from the feed |
| `externalSource` |  | Label stored on each episode for idempotent re-import (default: rss) |
| `status` |  | Status to import episodes as: draft (default) or published |
| `syncShow` |  | When importing into an existing show, set 1 to also overwrite the show metadata from the feed (cover, owner, category, ...). Default 0 = episodes only. |

### `podcast_upload_audio_from_url`

Fetch an audio file from a URL on the server and store it in the media library (avoids passing base64 through the client). Returns the hosted URL.

| Parameter | Type | Description |
| --- | --- | --- |
| `url` |  | Public URL of the audio file to fetch |
| `filename` |  | Filename to store as (default: derived from the URL) |
| `mimeType` |  | MIME type override (default: from the response Content-Type) |

## Apps (7)

### `app_list`

List iOS app landing pages, optionally filtered by status

| Parameter | Type | Description |
| --- | --- | --- |
| `status` |  | Filter by status: draft or published |
| `page` |  | Page number (default: 1) |
| `limit` |  | Items per page (default: 20) |

### `app_create`

Create a new iOS app landing page with name, description, features, and App Store info

| Parameter | Type | Description |
| --- | --- | --- |
| `name` |  | App name |
| `tagline` |  | Short tagline |
| `description` |  | Full description |
| `appStoreUrl` |  | App Store URL |
| `appStoreId` |  | App Store ID |
| `bundleId` |  | Bundle identifier |
| `platform` |  | Platform (default: iOS) |
| `features` |  | JSON array of feature objects [{icon, title, blurb}] |
| `screenshots` |  | JSON array of screenshot URLs |
| `links` |  | JSON object of additional links |
| `accentColor` |  | Accent color hex code |
| `icon` |  | App icon URL |
| `slug` |  | URL slug (auto-generated if not provided) |

### `app_publish`

Publish an app landing page so it appears on the public site

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | App ID |

### `app_update`

Update an app's editorial display info (tagline/features/accentColor/links/sortOrder/status/...). NOTE: description, screenshots, and icon are managed by app_sync (synced from the App Store) and are NOT editable here — editing them would be reverted on the next sync. After editing, run the build-trigger tool to render on /apps/:slug.

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | App ID |
| `name` |  | App name |
| `slug` |  | URL slug |
| `tagline` |  | Short tagline |
| `accentColor` |  | Accent color hex code |
| `features` |  | JSON array of feature objects [{icon, title, blurb}] |
| `links` |  | JSON object of additional links |
| `sortOrder` |  | Sort order |
| `status` |  | Status: draft or published |
| `meta` |  | JSON metadata string |

### `app_discover`

Discover apps from App Store Connect and create draft rows for new ones (idempotent; no sync, no publish, no ASC writeback).

_No parameters._

### `app_sync`

Sync an app's metadata from the App Store (rating/category/version/screenshots) using iTunes Lookup and App Store Connect

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | App ID to sync |

### `app_sync_all`

Sync metadata for all apps that have an App Store ID (rating/category/version/screenshots)

_No parameters._

## Pages (6)

### `page_list`

List WordBase pages (companion pages: privacy/terms/help/...). Optional `status` filters; omit it to list all.

| Parameter | Type | Description |
| --- | --- | --- |
| `status` |  | Filter by status: draft or published (omit for all) |

### `page_get`

Get a single companion page by ID or slug

| Parameter | Type | Description |
| --- | --- | --- |
| `idOrSlug` |  | Page ID or slug |

### `page_create`

Create a companion page (privacy/terms/help/...). Use slug convention <app>-<type> (e.g. delphi-privacy). Optional app arg stamps meta.appId for app↔page association.

| Parameter | Type | Description |
| --- | --- | --- |
| `title` |  | Page title |
| `content` |  | Page content in Markdown |
| `slug` |  | URL slug (recommended: <app>-<type>, e.g. delphi-privacy) |
| `sortOrder` |  | Sort order (default: 0) |
| `status` |  | Status: draft (default) or published |
| `meta` |  | JSON metadata string |
| `app` |  | App slug to associate this page with (stamps meta.appId) |

### `page_update`

Update a companion page

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Page ID |
| `title` |  | Page title |
| `slug` |  | URL slug |
| `content` |  | Page content in Markdown |
| `sortOrder` |  | Sort order |
| `status` |  | Status: draft or published |
| `meta` |  | JSON metadata string |

### `page_delete`

Delete a companion page

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Page ID |

### `page_publish`

Publish a companion page (sets status=published) and rebuilds the static site so it renders at its public URL.

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Page ID |

## category (4)

### `category_list`

List all categories with the number of posts in each (postCount).

_No parameters._

### `category_create`

Create a category. NOT idempotent — posting a duplicate slug returns isError (no create-or-attach).

| Parameter | Type | Description |
| --- | --- | --- |
| `name` |  | Category name |
| `slug` |  | URL slug (auto-generated from name if omitted) |
| `description` |  | Optional description |
| `sortOrder` |  | Sort order (default: 0) |

### `category_update`

Update a category's name, slug, description, or sortOrder. Rebuilds the static site only if the category is attached to at least one published post.

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Category ID |
| `name` |  | New category name |
| `slug` |  | New URL slug |
| `description` |  | New description |
| `sortOrder` |  | New sort order |

### `category_delete`

Delete a category. The DB cascades to drop the category from any posts that referenced it; rebuilds the static site only if the category was attached to at least one published post.

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Category ID |

## i18n (3)

### `i18n_render`

Render a published entity field in `lang` using the i18n cache. Unpublished entities, unknown ids, and unsupported (type, field) pairs return isError (no auth required for the underlying REST route, but MCP requires i18n:read).

| Parameter | Type | Description |
| --- | --- | --- |
| `type` |  | Entity type: post, page, or app |
| `id` |  | Entity ID or slug |
| `field` |  | Field to render: content (post/page markdown), title (post/page), tagline or features (app) |
| `lang` |  | Target language code (e.g. en, ja). Empty / "zh" returns the source. |

### `i18n_pending`

List translatable source units (across all published posts/pages/apps) that have no cache row for `lang`. Each item is { hash, text, ref }. Ref format: "type:id:field" (or "type:id:features.title|blurb").

| Parameter | Type | Description |
| --- | --- | --- |
| `lang` |  | Target language code (e.g. en, ja) |

### `i18n_put_cache`

Write translation entries to the i18n cache. Each entry is { sourceHash, lang, text, model, humanEdited }. Preserves the human_edited guard (AI writes cannot overwrite a human row). entries must be a JSON-encoded string of an array (the MCP schema only carries string/number).

| Parameter | Type | Description |
| --- | --- | --- |
| `entries` |  | JSON string: array of { sourceHash, lang, text, model, humanEdited } |

## post (9)

### `post_list`

List blog posts with optional filtering

| Parameter | Type | Description |
| --- | --- | --- |
| `status` |  | Filter by status: draft, published, archived |
| `category` |  | Filter by category slug |
| `tag` |  | Filter by tag slug |
| `page` |  | Page number (default: 1) |
| `limit` |  | Items per page (default: 10) |
| `search` |  | Search in post titles |

### `post_get`

Get a single blog post by ID or slug, including its tags and categories

| Parameter | Type | Description |
| --- | --- | --- |
| `idOrSlug` |  | Post ID or slug |

### `post_search`

Full-text search published blog posts (LIKE). Returns title + snippet.

| Parameter | Type | Description |
| --- | --- | --- |
| `q` |  | Search query (Chinese supported) |
| `limit` |  | Max results (default 20, max 50) |

### `post_create`

Create a new blog post

| Parameter | Type | Description |
| --- | --- | --- |
| `title` |  | Post title |
| `content` |  | Post content in Markdown |
| `slug` |  | URL slug (auto-generated if not provided) |
| `status` |  | Post status: draft (default) or published |
| `categoryIds` |  | Comma-separated category IDs |
| `tagIds` |  | Comma-separated tag IDs |

### `post_update`

Update a blog post (editorial fields: title/content/slug/excerpt/status/tags/categories). For SEO meta use post_update_meta.

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Post ID |
| `title` |  | New title |
| `content` |  | New Markdown content |
| `slug` |  | New slug (normalized) |
| `excerpt` |  | New excerpt |
| `status` |  | draft \| published \| archived |
| `categoryIds` |  | Comma-separated category IDs; empty string clears all, omit keeps |
| `tagIds` |  | Comma-separated tag IDs; empty string clears all, omit keeps |

### `post_publish`

Publish a draft blog post (idempotent: already-published returns as-is). Rebuilds the static site.

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Post ID |

### `post_archive`

Archive a blog post. Rebuilds the static site only if the post was previously published (so the live page is removed).

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Post ID |

### `post_delete`

Delete a blog post. Rebuilds the static site only if the post was previously published (so the live page is removed).

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Post ID |

### `post_update_meta`

Update SEO metadata for a post (og:title, og:description, og:image)

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Post ID |
| `description` |  | Meta description / og:description |
| `ogTitle` |  | og:title (defaults to post title) |
| `ogImage` |  | og:image URL |

## tag (4)

### `tag_list`

List all tags with the number of posts that use each (postCount).

_No parameters._

### `tag_create`

Create a tag. If a tag with the same slug already exists, returns the existing one (create-or-attach, idempotent).

| Parameter | Type | Description |
| --- | --- | --- |
| `name` |  | Tag name |
| `slug` |  | URL slug (auto-generated from name if omitted) |

### `tag_update`

Update a tag's name or slug. Rebuilds the static site only if the tag is attached to at least one published post.

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Tag ID |
| `name` |  | New tag name |
| `slug` |  | New URL slug |

### `tag_delete`

Delete a tag. The DB cascades to drop the tag from any posts that referenced it; rebuilds the static site only if the tag was attached to at least one published post.

| Parameter | Type | Description |
| --- | --- | --- |
| `id` |  | Tag ID |

