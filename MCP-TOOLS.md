# WordBase MCP — Tool Catalog

> Auto-generated by `pnpm mcp:catalog` — do not edit by hand.
> Source: `packages/api/src/mcp/tools.ts` · MCP server `wordbase-blog` v1.4.0 · 2026-09-05

**68 tools** across 14 groups.

## Index

- **Posts** (9): `post_list` · `post_get` · `post_search` · `post_create` · `post_update` · `post_publish` · `post_archive` · `post_delete` · `post_update_meta`
- **Tags** (5): `tag_list` · `tag_get` · `tag_create` · `tag_update` · `tag_delete`
- **Categories** (5): `category_list` · `category_get` · `category_create` · `category_update` · `category_delete`
- **Media** (5): `media_list` · `media_get` · `media_upload` · `media_delete` · `media_upload_from_url`
- **Comments** (4): `comment_list` · `comment_moderate` · `comment_reply` · `comment_delete`
- **Analytics** (1): `analytics_query`
- **Build** (2): `build_trigger` · `build_status`
- **Redirects** (1): `redirect_manage`
- **Podcast** (16): `podcast_list_shows` · `podcast_get_show` · `podcast_create_show` · `podcast_publish_show` · `podcast_delete_show` · `podcast_list_episodes` · `podcast_get_episode` · `podcast_analytics` · `podcast_get_feedback` · `podcast_create_episode` · `podcast_publish_episode` · `podcast_delete_episode` · `podcast_update_show` · `podcast_update_episode` · `podcast_import_feed` · `podcast_upload_audio_from_url`
- **Apps** (8): `app_list` · `app_get` · `app_create` · `app_publish` · `app_update` · `app_delete` · `app_discover` · `app_sync`
- **Pages** (6): `page_list` · `page_get` · `page_create` · `page_update` · `page_delete` · `page_publish`
- **Settings** (2): `settings_get_site` · `settings_update_site`
- **Observability** (1): `observability_query`
- **i18n** (3): `i18n_render` · `i18n_pending` · `i18n_put_cache`

## Posts (9)

### `post_list` — List blog posts

List blog posts with optional filtering. Returns summary fields only (no content/meta) — use post_get for the full post body.

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `status` | string | Filter by status. 用户说『只看草稿』时用 draft,『看已发布的』用 published,『看已下架的』用 archived. Example: status: 'published'. |
| `category` | string | Filter by category slug, e.g. category: 'life-notes'. |
| `tag` | string | Filter by tag slug, e.g. tag: 'ai'. |
| `page` | number | Page number, 1-based (default: 1). |
| `limit` | number | Items per page (default: 10, max 100). |
| `search` | string | Search in post titles, e.g. search: '发布'. |

### `post_get` — Get blog post

Get a single blog post by ID or slug, including its tags and categories

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `idOrSlug` | string | Post ID or slug. 用户说『打开/看看那篇 XX』时,先用 post_search 或 post_list 找到 id/slug 再传进来. Example: idOrSlug: 'rss-explained'. |

### `post_search` — Search blog posts

Full-text search published blog posts (LIKE). Returns title + snippet.

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `q` | string | Search query (Chinese supported). 用户说『搜一下关于 XX 的文章』时,q 就是 XX. Example: q: 'RSS'. |
| `limit` | number | Max results (default 20). The search service caps at 50 regardless of what is requested here — values above 50 are silently truncated by the service, not by this schema. |

### `post_create` — Create blog post

Create a new blog post with a title and Markdown content

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `title` | string | Post title. 用户说『写一篇关于 XX 的文章』时,XX 通常就是这个字段的值. Example: title: 'RSS 是什么'. |
| `content` | string | Post content in Markdown. 把用户口述/给定的正文直接转成 Markdown 传入. Example: content: '# 引言\n\n正文……'. |
| `slug` | string | URL slug (auto-generated if not provided). Example: slug: 'rss-explained'. |
| `status` | string | Post status. 用户说『先存草稿』时用 draft(默认);说『直接发布』/『上线』时用 published. Example: status: 'draft'. |
| `categoryIds` | array | Category IDs to attach — pass an array like ["cat1","cat2"] (a comma-separated string is also accepted). 用户说『放进随笔分类』时,先用 category_list 查到 id 再传进来. |
| `tagIds` | array | Tag IDs to attach — pass an array like ["tag1","tag2"] (a comma-separated string is also accepted). 用户说『加上『AI』标签』时,先用 tag_list 或 tag_create 拿到 id 再传进来. |

### `post_update` — Update blog post

Update a blog post (editorial fields: title/content/slug/excerpt/status/tags/categories). For SEO meta use post_update_meta.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Post ID. |
| `title` | string | New title. |
| `content` | string | New Markdown content. |
| `slug` | string | New slug (normalized). |
| `excerpt` | string | New excerpt. |
| `status` | string | 用户说『先存草稿』→ draft;『发布』/『上线』→ published;『下架』→ archived. Example: status: 'archived'. |
| `categoryIds` | array | Category IDs — a full-replacement array (comma-separated string also accepted), not an append: passing it replaces the whole set. Pass an empty array to clear all; omit the field entirely to keep the current set unchanged. |
| `tagIds` | array | Tag IDs — a full-replacement array (comma-separated string also accepted), not an append: passing it replaces the whole set. Pass an empty array to clear all; omit the field entirely to keep the current set unchanged. |

### `post_publish` — Publish blog post

Publish a draft blog post (idempotent: already-published returns as-is). Rebuilds the static site.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `idempotentHint: true` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Post ID. |

### `post_archive` — Archive blog post

Archive a blog post. Rebuilds the static site only if the post was previously published (so the live page is removed).

_Annotations:_ `readOnlyHint: false` · `destructiveHint: true` · `idempotentHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Post ID. |

### `post_delete` — Delete blog post

Delete a blog post. Rebuilds the static site only if the post was previously published (so the live page is removed).

_Annotations:_ `readOnlyHint: false` · `destructiveHint: true` · `idempotentHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Post ID. |

### `post_update_meta` — Update post SEO metadata

Update SEO metadata for a post (og:title, og:description, og:image)

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Post ID. |
| `description` | string | Meta description / og:description. 用户说『改一下这篇的 SEO 描述』时用这个. Example: description: '一篇讲 RSS 原理的入门文章'. |
| `ogTitle` | string | og:title (defaults to post title). Example: ogTitle: 'RSS 入门指南'. |
| `ogImage` | string | og:image URL. Example: ogImage: 'https://norvyn.com/media/cover.png'. |

## Tags (5)

### `tag_list` — List tags

List all tags with the number of posts that use each (postCount).

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

_No parameters._

### `tag_get` — Get tag

Get a single tag by ID or slug.

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `idOrSlug` | string | Tag ID or slug. Example: idOrSlug: 'ai'. |

### `tag_create` — Create tag

Create a tag. If a tag with the same slug already exists, returns the existing one (create-or-attach, idempotent).

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `idempotentHint: true` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `name` | string | Tag name. 用户说『加上『随笔』标签』时,name 就是『随笔』. Example: name: '随笔'. |
| `slug` | string | URL slug (auto-generated from name if omitted). |

### `tag_update` — Update tag

Update a tag's name or slug. Rebuilds the static site only if the tag is attached to at least one published post.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Tag ID. |
| `name` | string | New tag name. 用户说『把标签改叫 XX』时,name 就是 XX. |
| `slug` | string | New URL slug. |

### `tag_delete` — Delete tag

Delete a tag. The DB cascades to drop the tag from any posts that referenced it; rebuilds the static site only if the tag was attached to at least one published post.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: true` · `idempotentHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Tag ID. |

## Categories (5)

### `category_list` — List categories

List all categories with the number of posts in each (postCount).

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

_No parameters._

### `category_get` — Get category

Get a single category by ID or slug.

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `idOrSlug` | string | Category ID or slug. Example: idOrSlug: 'life-notes'. |

### `category_create` — Create category

Create a category. NOT idempotent — posting a duplicate slug returns isError (no create-or-attach).

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `name` | string | Category name. 用户说『新建一个『生活笔记』分类』时,name 就是『生活笔记』. Example: name: '生活笔记'. |
| `slug` | string | URL slug (auto-generated from name if omitted). |
| `description` | string | Optional description. |
| `sortOrder` | number | Sort order (default: 0). |

### `category_update` — Update category

Update a category's name, slug, description, or sortOrder. Rebuilds the static site only if the category is attached to at least one published post.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Category ID. |
| `name` | string | New category name. 用户说『把分类改叫 XX』时,name 就是 XX. |
| `slug` | string | New URL slug. |
| `description` | string | New description. |
| `sortOrder` | number | New sort order. |

### `category_delete` — Delete category

Delete a category. The DB cascades to drop the category from any posts that referenced it; rebuilds the static site only if the category was attached to at least one published post.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: true` · `idempotentHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Category ID. |

## Media (5)

### `media_list` — List media library items

List media library items (uploaded images and files), paginated

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `page` | number | Page number, 1-based (default: 1). |
| `limit` | number | Items per page (default: 20, max 100). |

### `media_get` — Get media item

Get a single media library item by ID.

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Media ID. |

### `media_upload` — Upload media

Upload a file to the media library (base64 encoded)

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `filename` | string | Original filename, e.g. filename: 'cover.png'. |
| `content` | string | Base64 encoded file content — raw base64 only, do not include a data: URI prefix (e.g. no "data:image/png;base64," before it). Keep uploads small (a few MB); large files should not be pushed through the model context. |
| `mimeType` | string | MIME type, e.g. mimeType: 'image/png' or 'image/jpeg'. |
| `altText` | string | Alt text for the image (optional). Example: altText: '封面图'. |

### `media_delete` — Delete media item

Delete a media item from the media library by ID

_Annotations:_ `readOnlyHint: false` · `destructiveHint: true` · `idempotentHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Media ID to delete. |

### `media_upload_from_url` — Upload media from URL

Fetch a file from a URL on the server and store it in the media library (avoids passing base64 through the client). Returns the hosted media record.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: true`

| Parameter | Type | Description |
| --- | --- | --- |
| `url` | string | Public URL of the file to fetch |
| `filename` | string | Filename to store as (default: derived from the URL) |
| `mimeType` | string | MIME type override (default: from the response Content-Type) |
| `altText` | string | Alt text for the image (optional). Example: altText: '封面图'. |

## Comments (4)

### `comment_list` — List post comments

List comments for a blog post, optionally filtered by status

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `postId` | string | Post ID to list comments for. |
| `status` | string | Filter by status (default: approved). 用户说『看看待审的评论』时用 pending,『看看垃圾评论』用 spam. Example: status: 'pending'. |
| `page` | number | Page number, 1-based (default: 1). |
| `limit` | number | Items per page (default: 20, max 100). |

### `comment_moderate` — Moderate comment

Moderate a comment (approve, spam, or trash)

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Comment ID to moderate. |
| `action` | string | 用户说『通过这条评论』用 approve,『标成垃圾』用 spam,『删掉/丢进回收站』用 trash. Example: action: 'approve'. |

### `comment_reply` — Reply to comment

Reply to an existing comment as the site owner

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `postId` | string | Post ID the comment belongs to. |
| `parentId` | string | Parent comment ID to reply to. |
| `authorName` | string | Author name shown on the reply when the site owner replies. Example: authorName: '站长'. |
| `content` | string | Reply content (plain text). |

### `comment_delete` — Delete comment

Delete a comment from a blog post by ID

_Annotations:_ `readOnlyHint: false` · `destructiveHint: true` · `idempotentHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Comment ID to delete. |

## Analytics (1)

### `analytics_query` — Query blog analytics

Query blog analytics. `sections` selects which parts to return (default: all): overview (total PV, today PV, active posts), top_posts (ranked by page views), trends (traffic over time, bucketed by period), content_stats (publish frequency + tag distribution).

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `sections` | array | Which sections to return (default: all four). Example: sections: ['overview', 'top_posts']. |
| `limit` | number | Number of top posts to return when top_posts is included (default: 10, max 100). |
| `period` | string | Period bucket for trends when included. 用户问『最近流量趋势』时默认用 daily;问『这几周』用 weekly;问『按月看』用 monthly. Example: period: 'weekly'. |

## Build (2)

### `build_trigger` — Trigger site rebuild

Trigger an Astro static site rebuild for the blog

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `idempotentHint: true` · `openWorldHint: false`

_No parameters._

### `build_status` — Get build status

Check the current Astro site build status

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

_No parameters._

## Redirects (1)

### `redirect_manage` — Manage URL redirects

List, create, or delete URL redirects. 'list' needs no other fields. 'create' needs fromPath + toPath. 'delete' needs id.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `action` | string | Action to perform. 用户说『看看有哪些跳转』用 list;『加一条跳转』用 create(还需 fromPath+toPath);『删掉这条跳转』用 delete(还需 id). |
| `fromPath` | string | Source path, required for create. Example: fromPath: '/old-slug'. |
| `toPath` | string | Target path, required for create. Example: toPath: '/new-slug'. |
| `id` | string | Redirect ID, required for delete. |

## Podcast (16)

### `podcast_list_shows` — List podcast shows

List all podcast shows, optionally filtered by status

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `status` | string | Filter by status: draft or published |
| `page` | number | Page number (default: 1) |
| `limit` | number | Items per page (default: 20) |

### `podcast_get_show` — Get podcast show

Get a single podcast show by ID or slug.

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `idOrSlug` | string | Podcast show ID or slug. Example: idOrSlug: 'the-show'. |

### `podcast_create_show` — Create podcast show

Create a new podcast show with title, description, and owner info

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `title` | string | Show title |
| `description` | string | Show description |
| `ownerEmail` | string | Owner email for iTunes feed |
| `ownerName` | string | Owner name for iTunes feed |
| `author` | string | Author name |
| `language` | string | Language code (default: zh-CN) |
| `category` | string | iTunes category |
| `explicit` | boolean | Explicit content flag. Accepts true/false (also accepts legacy 1/0). |
| `coverImage` | string | Cover image URL |
| `appleUrl` | string | Apple Podcasts show URL |
| `spotifyUrl` | string | Spotify show URL |
| `slug` | string | URL slug (auto-generated if not provided) |

### `podcast_publish_show` — Publish podcast show

Publish a podcast show so it appears on the public site

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `idempotentHint: true` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Podcast show ID |

### `podcast_delete_show` — Delete podcast show

Delete a podcast show. Rebuilds the static site only if the show was previously published (so the live show page and its RSS feed are removed).

_Annotations:_ `readOnlyHint: false` · `destructiveHint: true` · `idempotentHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Podcast show ID. |

### `podcast_list_episodes` — List podcast episodes

List episodes for a podcast show, optionally filtered by status. Returns summary fields only — excludes transcript and full show notes to keep the response small.

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `podcastId` | string | Podcast show ID |
| `status` | string | Filter by status: draft or published |
| `page` | number | Page number (default: 1) |
| `limit` | number | Items per page (default: 20) |

### `podcast_get_episode` — Get podcast episode

Get a single podcast episode by ID or slug, including transcript and full show notes.

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `idOrSlug` | string | Episode ID or slug. Example: idOrSlug: 'ep-42'. |

### `podcast_analytics` — Podcast analytics

Podcast consumption analytics: deduped downloads (total + windowed), active-subscriber estimate, top episodes, and feed-poll client distribution

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `days` | number | Window in days for windowed downloads (default: 30) |
| `limit` | number | Max top episodes / clients to return (default: 10) |

### `podcast_get_feedback` — Get listener feedback

Get recent listener feedback (reaction + category + note) across episodes, for podcast topic-reward and viewpoint-falsification signals. Read-only.

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `days` | number | Look back this many days (default: 7). Ignored if `since` is given. |
| `since` | number | Unix epoch seconds; return feedback created at or after this time. |
| `episodeId` | string | Filter to a single episode id. |
| `limit` | number | Max rows (default: 200, max: 1000). |

### `podcast_create_episode` — Create or update podcast episode

Create or update a podcast episode. Only idempotent when both externalSource and externalId are given — that combination upserts by external id and is safe for automated re-delivery. Without them, every call creates a new episode.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `idempotentHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `podcastId` | string | Podcast show ID |
| `title` | string | Episode title |
| `audioUrl` | string | URL of the audio file. Required when creating a new episode; may be omitted when updating an existing one via the externalSource+externalId upsert. |
| `audioSize` | number | Audio file size in bytes |
| `duration` | number | Duration in seconds |
| `summary` | string | Short summary |
| `showNotes` | string | Full show notes (Markdown) |
| `episodeNumber` | number | Episode number |
| `seasonNumber` | number | Season number |
| `coverImage` | string | Episode cover image URL |
| `transcript` | string | Transcript text |
| `episodeType` | string | Episode type: full, trailer, or bonus |
| `explicit` | boolean | Explicit content flag. Accepts true/false (also accepts legacy 1/0). |
| `publishedAt` | number | Publish date as a unix epoch (seconds) |
| `audioType` | string | Audio MIME type, e.g. audio/mpeg, audio/wav, audio/mp4 (defaults to audio/mpeg) |
| `externalSource` | string | External source identifier (e.g. "adam"). Give this together with externalId to make the call an idempotent upsert. |
| `externalId` | string | External episode ID. Give this together with externalSource to make the call an idempotent upsert — safe to retry. |
| `slug` | string | URL slug (auto-generated if not provided) |

### `podcast_publish_episode` — Publish podcast episode

Publish a podcast episode so it appears in the RSS feed and on the site

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `idempotentHint: true` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Episode ID |

### `podcast_delete_episode` — Delete podcast episode

Delete a podcast episode. Rebuilds the static site only if the episode was previously published (so the live episode page and RSS feed entry are removed).

_Annotations:_ `readOnlyHint: false` · `destructiveHint: true` · `idempotentHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Episode ID. |

### `podcast_update_show` — Update podcast show

Update a podcast show's metadata (title/description/slug/author/owner/category/cover/etc.). Triggers a site rebuild.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Podcast show ID |
| `title` | string | Show title |
| `description` | string | Show description |
| `slug` | string | URL slug |
| `author` | string | Author name |
| `ownerName` | string | Owner name for iTunes feed |
| `ownerEmail` | string | Owner email for iTunes feed |
| `category` | string | iTunes category |
| `coverImage` | string | Cover image URL |
| `language` | string | Language code (e.g. zh-CN) |
| `copyright` | string | Copyright line |
| `link` | string | Show website link |
| `appleUrl` | string | Apple Podcasts show URL |
| `spotifyUrl` | string | Spotify show URL |
| `explicit` | boolean | Explicit content flag. Accepts true/false (also accepts legacy 1/0). |
| `status` | string | Status: draft or published |

### `podcast_update_episode` — Update podcast episode

Update a podcast episode (title/summary/show notes/transcript/episode number/type/publishedAt/etc.). Rebuilds the site when the episode is published.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Episode ID |
| `title` | string | Episode title |
| `summary` | string | Short summary |
| `showNotes` | string | Full show notes (HTML or Markdown) |
| `transcript` | string | Transcript text |
| `episodeNumber` | number | Episode number |
| `seasonNumber` | number | Season number |
| `episodeType` | string | Episode type: full, trailer, or bonus |
| `explicit` | boolean | Explicit content flag. Accepts true/false (also accepts legacy 1/0). |
| `duration` | number | Duration in seconds |
| `coverImage` | string | Episode cover image URL |
| `publishedAt` | number | Publish date as a unix epoch (seconds) |
| `status` | string | Status: draft or published |

### `podcast_import_feed` — Import podcast RSS feed

Import an external podcast RSS feed (e.g. Anchor/Spotify): upserts every episode (idempotent by guid), keeping the original audio URLs and publish dates. Episodes import as draft unless status=published. When importing into an EXISTING show, the show's own metadata (title/cover/owner/etc.) is left untouched unless syncShow=true.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `idempotentHint: true` · `openWorldHint: true`

| Parameter | Type | Description |
| --- | --- | --- |
| `feedUrl` | string | URL of the external RSS feed |
| `podcastId` | string | Target show ID to import into; if omitted, a new show is created from the feed |
| `externalSource` | string | Label stored on each episode for idempotent re-import (default: rss) |
| `status` | string | Status to import episodes as: draft (default) or published |
| `syncShow` | boolean | When importing into an existing show, set true to also overwrite the show metadata from the feed (cover, owner, category, ...). Default false = episodes only. |

### `podcast_upload_audio_from_url` — Upload podcast audio from URL

Fetch an audio file from a URL on the server and store it in the media library (avoids passing base64 through the client). Returns the hosted URL.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: true`

| Parameter | Type | Description |
| --- | --- | --- |
| `url` | string | Public URL of the audio file to fetch |
| `filename` | string | Filename to store as (default: derived from the URL) |
| `mimeType` | string | MIME type override (default: from the response Content-Type) |

## Apps (8)

### `app_list` — List apps

List iOS app landing pages, optionally filtered by status

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `status` | string | Filter by status. 用户说『看已发布的 app』时用 published,『看草稿』用 draft. Example: status: 'published'. |
| `page` | number | Page number, 1-based (default: 1). |
| `limit` | number | Items per page (default: 20, max 100). |

### `app_get` — Get app

Get a single iOS app landing page by ID or slug.

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `idOrSlug` | string | App ID or slug. Example: idOrSlug: 'delphi'. |

### `app_create` — Create app landing page

Create a new iOS app landing page with name, description, features, and App Store info

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `name` | string | App name. Example: name: 'Delphi'. |
| `tagline` | string | Short tagline shown under the app name. |
| `description` | string | Full description. |
| `appStoreUrl` | string | App Store URL. |
| `appStoreId` | string | App Store numeric ID. |
| `bundleId` | string | Bundle identifier, e.g. com.example.app. |
| `platform` | string | Platform (default: 'iOS'). |
| `features` | array | Feature bullets shown on the landing page. 用户说『给它加三个卖点』时,每条卖点是一个 {icon, title, blurb} 对象. Example: [{ icon: '⚡', title: 'Fast', blurb: 'Starts in under a second.' }]. |
| `screenshots` | array | Screenshot URLs, in display order. |
| `links` | object | Additional links keyed by label, e.g. { website: 'https://...', support: 'https://...' }. Free-form: any key/value pair is kept as-is. |
| `accentColor` | string | Accent color hex code, e.g. #5B8DEF. |
| `icon` | string | App icon URL. |
| `slug` | string | URL slug (auto-generated from name if not provided). |

### `app_publish` — Publish app landing page

Publish an app landing page so it appears on the public site

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `idempotentHint: true` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | App ID (get it from app_list or app_create). Example: id: 'app_abc123'. |

### `app_update` — Update app landing page

Update an app's editorial display info (tagline/features/accentColor/links/sortOrder/status/...). NOTE: description, screenshots, and icon are managed by app_sync (synced from the App Store) and are NOT editable here — editing them would be reverted on the next sync. After editing, run the build-trigger tool to render on /apps/:slug.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | App ID (get it from app_list). Example: id: 'app_abc123'. |
| `name` | string | App name. |
| `slug` | string | URL slug. |
| `tagline` | string | Short tagline. 用户说『改一下标语』时用这个. Example: tagline: 'Your ideas, organized.'. |
| `accentColor` | string | Accent color hex code, e.g. #5B8DEF. |
| `features` | array | Feature bullets shown on the landing page. 传了就是整组替换,不是追加. 用户说『给它加三个卖点』『把卖点改成……』时用这个,并且要带上想保留的旧卖点. Example: [{ icon: '⚡', title: 'Fast', blurb: 'Starts in under a second.' }]. |
| `links` | object | Additional links keyed by label, e.g. { website: 'https://...', support: 'https://...' }. 传了就是整组替换,不是追加. Free-form: any key/value pair is kept as-is. |
| `sortOrder` | number | Sort order among app listings (lower shows first). |
| `status` | string | 用户说『下架这个 app』时用 draft,『上线/发布』用 published. Example: status: 'published'. |
| `meta` | string | JSON metadata string. |

### `app_delete` — Delete app

Delete an app landing page. Rebuilds the static site only if the app was previously published (so the live /apps/:slug page is removed).

_Annotations:_ `readOnlyHint: false` · `destructiveHint: true` · `idempotentHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | App ID (get it from app_list). Example: id: 'app_abc123'. |

### `app_discover` — Discover apps from App Store Connect

Discover apps from App Store Connect and create draft rows for new ones (idempotent; no sync, no publish, no ASC writeback).

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `idempotentHint: true` · `openWorldHint: true`

_No parameters._

### `app_sync` — Sync app metadata from App Store

Sync app metadata from the App Store (version/category/screenshots/rating/icon) via iTunes Lookup + App Store Connect, then rebuild the site if anything changed. Give `id` to sync ONE app; OMIT `id` to sync EVERY app that has an App Store ID. Returns the fields that actually changed per app — an empty `fields` list means the fetch succeeded and nothing differed, which is a normal outcome, not a failure.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `idempotentHint: true` · `openWorldHint: true`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | App ID to sync. OMIT this to sync all apps instead. 用户说『同步一下 Delphi』时传它对应的 id;说『同步所有 app』/『刷新 App Store 数据』时不要传. Example: id: 'app_abc123'. |

## Pages (6)

### `page_list` — List pages

List WordBase pages (companion pages: privacy/terms/help/...). Optional `status` filters; omit it to list all.

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `status` | string | Filter by status. 用户说『看已发布的页面』时用 published,『看草稿』用 draft(omit for all). Example: status: 'published'. |

### `page_get` — Get page

Get a single companion page by ID or slug

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `idOrSlug` | string | Page ID or slug. Example: idOrSlug: 'delphi-privacy'. |

### `page_create` — Create page

Create a companion page for an app (privacy / terms / support). CONVENTION — you are expected to be calling this from inside the app's own project, so name the page after THAT project, not after the App Store listing: slug = `<project-slug>-<type>`, where <project-slug> is the project/repo name lowercased (e.g. project CleanLabel → `cleanlabel-privacy`, `cleanlabel-terms`, `cleanlabel-support`) and <type> is one of privacy | terms | support. Do NOT derive the slug from the App Store display name — that name changes for ASO reasons and the page URL must stay stable (it is submitted to App Store Connect as the app's Support/Privacy URL). App Store Connect requires a Support URL and a Privacy Policy URL for every app, so privacy + support are the minimum pair. Recommended: also pass `app` so meta.appId records which app this page belongs to — WordBase does not enforce or infer the association, the calling project owns it.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `title` | string | Page title. Example: title: 'Privacy Policy'. |
| `content` | string | Page content in Markdown. Example: content: '# Privacy Policy\n\n...'. |
| `slug` | string | URL slug. Convention: `<project-slug>-<type>` from the CALLING PROJECT's name (lowercased), type ∈ privacy\|terms\|support. Example: slug: 'cleanlabel-support'. Keep it stable once submitted to App Store Connect. |
| `sortOrder` | number | Sort order (default: 0). |
| `status` | string | 用户说『先存草稿』时用 draft(默认);说『直接发布』时用 published. Example: status: 'draft'. |
| `meta` | object | Structured metadata object, e.g. { appId: "delphi" }. Merged with the app arg below, not replaced by it. |
| `app` | string | App slug to associate this page with (stamps meta.appId). Example: app: 'delphi'. |

### `page_update` — Update page

Update a companion page's title, content, or status

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Page ID. |
| `title` | string | Page title. |
| `slug` | string | URL slug. |
| `content` | string | Page content in Markdown. |
| `sortOrder` | number | Sort order. |
| `status` | string | 用户说『下架这个页面』时用 draft,『发布/上线』用 published. Example: status: 'published'. |
| `meta` | object | 传了就是整个替换,不是合并. Structured metadata object, e.g. { appId: "delphi" }. |

### `page_delete` — Delete page

Delete a companion page (privacy/terms/help/...) by ID

_Annotations:_ `readOnlyHint: false` · `destructiveHint: true` · `idempotentHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Page ID. |

### `page_publish` — Publish page

Publish a companion page (sets status=published) and rebuilds the static site so it renders at its public URL.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `idempotentHint: true` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | string | Page ID. |

## Settings (2)

### `settings_get_site` — Get site identity

Get the resolved site identity (name/description/author/email/github) shown in nav, footer, RSS, and llms.txt meta. This is a defaults+settings overlay view, not the raw settings map.

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

_No parameters._

### `settings_update_site` — Update site identity

Update site identity fields (name/description/author/email/github) shown in nav, footer, RSS, and llms.txt meta. Only writes these five whitelisted keys, not the rest of the settings map. Rebuilds the static site (site identity is baked into every page's meta). Known quirk: a legacy placeholder value ("Wordbase Blog" / "A personal blog") is silently replaced by the default on read, so the value read back after writing may differ from what you wrote.

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `name` | string | Site name. Example: name: 'norvyn'. |
| `description` | string | Site tagline/description — drives RSS, llms.txt, and Organization JSON-LD. |
| `author` | string | Site author name. |
| `email` | string | Contact email shown in footer/About. |
| `github` | string | GitHub profile URL. |

## Observability (1)

### `observability_query` — Query observability data

Query observability data: visitor analytics (visits/trends/top-pages/referrers/shares/regions/devices), content stats, request metrics, system status, SEO health, and podcast consumption analytics (podcast-summary/trends/top-episodes/episodes/clients). `section` selects which one to return.

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `section` | string | Which section to query. 用户问『上周哪些国家来的人多』用 regions;『流量趋势』用 trends;『播客下载』用 podcast-summary. Example: section: 'regions'. |
| `days` | number | Window in days, for sections that support it (default varies by section; ignored otherwise). |
| `limit` | number | Max rows to return, for sections that support it (default varies by section; ignored otherwise). |

## i18n (3)

### `i18n_render` — Render translated entity field

Render a published entity field in `lang` using the i18n cache. Unpublished entities, unknown ids, and unsupported (type, field) pairs return isError (no auth required for the underlying REST route, but MCP requires i18n:read).

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `type` | string | Entity type. Example: type: 'page'. |
| `id` | string | Entity ID or slug. Example: id: 'delphi-privacy'. |
| `field` | string | Field to render: content (post/page markdown), title (post/page), tagline or features (app). |
| `lang` | string | Target language code (e.g. en, ja). Empty / "zh" returns the source. |

### `i18n_pending` — List pending translation units

List translatable source units (across all published posts/pages/apps) that have no cache row for `lang`. Each item is { hash, text, ref }. Ref format: "type:id:field" (or "type:id:features.title|blurb").

_Annotations:_ `readOnlyHint: true` · `destructiveHint: false` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `lang` | string | Target language code (e.g. en, ja). Example: lang: 'en'. |

### `i18n_put_cache` — Write translation cache entries

Write translation entries to the i18n cache. Each entry is { sourceHash, lang, text, model, humanEdited }. Preserves the human_edited guard (AI writes cannot overwrite a human row). 用户说『把这批翻译灌进去』时用这个:先用 i18n_pending 拿到每条的 hash,再逐条给出翻译文本. Pass a structured array (a JSON-encoded string of the same array is also accepted for backward compatibility).

_Annotations:_ `readOnlyHint: false` · `destructiveHint: false` · `idempotentHint: true` · `openWorldHint: false`

| Parameter | Type | Description |
| --- | --- | --- |
| `entries` | array | Translation entries. Example: [{ sourceHash: 'h1', lang: 'en', text: '[en] heading', model: 'claude', humanEdited: false }]. |

